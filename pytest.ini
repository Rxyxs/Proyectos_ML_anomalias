[pytest]
# pytest invocado a secas no agrega la raiz del repositorio a sys.path; solo python -m pytest
# lo hace. Sin esta linea "import src" falla en la recoleccion. Localmente siempre se corrio
# con python -m pytest y pasaba; CI invoca pytest directo y fallaba en 3.10 y 3.12 por esto.
pythonpath = .
testpaths = tests
